#include <stdio.h>

int main() {
    int T, N;
    
    
    scanf("%d", &T);
    
    
    for (int i = 0; i < T; i++) {
        
        scanf("%d", &N);
        
        
        int choices = N * (N - 1);
        
        
        printf("%d\n", choices);
    }
    
    return 0;
}
