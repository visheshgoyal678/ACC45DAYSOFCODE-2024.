#include <iostream>
using namespace std;


int minimum_coins(int N) {
  
    return (N / 5) * 4 + (N % 5);
}

int main() {
    int T; 
    cin >> T;

    
    while (T--) {
        int N;
        cin >> N;
        cout << minimum_coins(N) << endl;
    }

    return 0;
}
